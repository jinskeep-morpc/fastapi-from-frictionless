fields:
  - name: macaddr
    type: string
    constraints:
      required: true
  - name: serialnum
    type: string
    constraints:
      required: true
  - name: status
    type: string
    constraints:
      required: true
  - name: ssid
    type: string
    constraints:
      required: true
  - name: password
    type: string
    constraints:
      required: true
  - name: deployment_name
    type: string
primaryKey:
  - macaddr
foreignKeys:
  - fields:
      - deployment_name
    reference:
      resource: deployment
      fields:
        - name
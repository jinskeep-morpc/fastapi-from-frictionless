# Windows VM configuration — copy to .env and adjust if needed
WINDOWS_VERSION=11
RAM_SIZE=8G
CPU_CORES=2
DISK_SIZE=64G
WIN_USERNAME=User
WIN_PASSWORD=test

# Host ports for accessing the VM
NOVNC_PORT=8006   # browser UI: http://localhost:8006
RDP_PORT=3389     # RDP client: localhost:3389
